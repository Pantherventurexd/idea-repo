import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.bystronic.com',"www.financialexpress.com", 'avatars.mds.yandex.net'], // Add this line
  },
  /* config options here */
};

export default nextConfig;
